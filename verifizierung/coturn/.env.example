# Kopiere diese Datei nach ".env" und trage echte Werte ein.
#
#   cp .env.example .env
#
# TURN_SECRET MUSS identisch sein mit der gleichnamigen Variable bei der App
# (in Coolify). Erzeugen z. B. mit:  openssl rand -hex 32

TURN_SECRET=hier_dein_langes_geheimnis_einsetzen
TURN_REALM=verify.4ever1.tv

# Öffentliche IPv4 deines Hetzner-Servers (aus der Hetzner Console)
EXTERNAL_IP=178.104.82.169
