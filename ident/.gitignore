node_modules/
package-lock.json
*.log

# Geheimnisse / lokale Umgebung
.env
.env.*
!.env.example

# Lokale (verschlüsselte) Daten – nie committen
data/

# coturn: echte Konfig enthält das Geheimnis – nur das .example committen
coturn/turnserver.conf
